server:
  port: 8080
  
spring:
  profiles:
    active: dev
    
  application:
    name: gateway
    
  cloud:
    config:
      uri: http://192.168.219.107:8088/
  
  rabbitmq:
    host: localhost
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: ['refresh']